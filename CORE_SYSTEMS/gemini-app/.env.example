# Gemini app — environment template (PLACEHOLDERS ONLY — safe to commit)
# ---------------------------------------------------------------------------
# Copy this file to ".env" (which is git-ignored) and set your real key there.
# NEVER commit the real .env. Get a key: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=YOUR_GOOGLE_API_KEY_HERE
