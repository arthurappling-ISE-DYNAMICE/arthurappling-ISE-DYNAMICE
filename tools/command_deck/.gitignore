# SYS_OS (command_deck) — local-only / secret-bearing files (NEVER commit)

# Operator's private runtime config — may contain a real Supabase anon key + URL.
# The committed template is assets/js/config.local.example.js (placeholders only,
# intentionally tracked). Only the REAL file below is ignored.
assets/js/config.local.js

# Backup / restore exports (may contain live operational data)
*.backup.json
sysos-backup-*.json

# Environment / secrets
.env
.env.*

# v4.7 — deploy/public/ is now DELIBERATELY TRACKED (public pilot activation,
# operator business-content signoff recorded 2026-07-06; repo confirmed PUBLIC).
# The artifact is generated by scripts/build_public_deploy.js and gated by
# scripts/validate_public_deploy.js — VALIDATION PASS is required before every
# commit that touches it. config.public.js carries the browser-safe anon key
# ONLY (RLS machine-verified); server keys abort the build.

# Local vendored SDK + local test page (never commit; deploy build makes its own copy)
assets/js/supabase.local.js
index.local.html
