node_modules/
gemini-app/node_modules/
.env
*.local
dist/
.DS_Store

# Vault & Archive binaries — stored in OneDrive/Drive, not source control
vault/**/*.pdf
vault/**/*.zip
vault/**/*.docx
vault/**/*.doc
vault/**/*.xlsx
vault/**/*.png
vault/**/*.jpg
vault/**/*.jpeg
vault/**/*.ico
vault/**/*.mp4

archived_old_versions/**/*.pdf
archived_old_versions/**/*.zip
archived_old_versions/**/*.docx
archived_old_versions/**/*.png
archived_old_versions/**/*.jpg
archived_old_versions/**/*.jpeg
archived_old_versions/**/*.ico

# Large asset files project-wide
*.zip
prompts/**/*.png
prompts/**/*.jpg
tools/consulting_wing/public/*.jpg
tools/consulting_wing/public/*.png
tools/consulting_wing/sovereign-promo/**/*.jpg
tools/consulting_wing/sovereign-promo/**/*.png

# Claude worktrees (internal only)
.claude/worktrees/

# Local SQLite vault — provisioned by tools/initialize_vault.py
data/

# =====================================================================
# v3.4 SAFETY SHIELD (2026-06-16) — prevent PII / personal / bloat / junk
# from ever being staged. Already-committed paths (e.g. tools/command_deck/
# build/) are unaffected; .gitignore does not untrack tracked files.
# =====================================================================

# Personal / legal / private data
cases_matrix.csv
*.docx
WorkOrders/
projects/

# Local assistant / tool configs
.claude/
.agents/

# Node / dependency bloat
node_modules/
**/node_modules/

# Build/cache artifacts
dist/
build/
.cache/
.temp/
tmp/
.DS_Store
Thumbs.db

# Accidental command typo files
add
commit
*.mdgit

# Environment / secrets
.env
.env.*
*.pem
*.key
*.crt
*.p12
*.pfx

# Logs
*.log
logs/
